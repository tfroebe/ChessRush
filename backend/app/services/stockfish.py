from typing import Optional

def evaluate_position_after_move(
    fen: str,
    uci_move: str
) -> Optional[float]:
    """
    Temporary stub for Stockfish evaluation.

    Returns:
    - None to disable engine-based filtering
    - Or a float representing pawn loss (lower is better)
    """

    # TODO: Replace with real Stockfish integration
    return None